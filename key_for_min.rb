# prereqs: iterators, hashes, conditional logic
# Given a hash with numeric values, return the key for the smallest value

def key_for_min_value(name_hash)
  smallest = ""
  smallest_key = ""
  names.each do |name, price|
    if price < smallest
      smallest = price
      smallest_key = name
  if name == #price is the lowest
    smallest = item
  end
end

smallest
end
